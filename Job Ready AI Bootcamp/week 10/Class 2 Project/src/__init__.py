"""Local Customer Feedback Analyzer — source package.

Sub-packages:
    sentiment  — the lazy-loaded HuggingFace sentiment pipeline wrapper.
    ingestion  — CSV loading and text-column detection.
    analytics  — aggregation of per-review results into dashboard summaries.
    utils      — cross-cutting helpers (logging).
"""
